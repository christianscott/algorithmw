{
  "name": "algorithmw",
  "version": "0.1.0",
  "description": "OCaml workflow with Esy",
  "license": "MIT",
  "esy": {
    "build": "dune build -p #{self.name}",
    "release": {
      "bin": "algorithmw",
      "includePackages": [
        "root",
        "@opam/base"
      ]
    }
  },
  "scripts": {
    "test": "esy x algorithmw"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@opam/base": "*",
    "ocaml": "~4.6.0"
  },
  "devDependencies": {
    "@opam/merlin": "^3.0.3",
    "ocaml": "~4.6.0"
  }
}
